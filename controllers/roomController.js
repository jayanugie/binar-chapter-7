const router = require('express').Router();

const { room } = require('../models');

// membuat room, input nama ruangan, memakain token yg sudah register
router.post('/room', (req, res) => {

})
